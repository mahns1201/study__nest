// model 정의는 class or interface로 사용이 가능하다.
// interface는 변수의 타입만 정의할 때 사용한다.
// class는 변수의 타입 체크 + 인스턴스 생성

// study__nest 코드에서는 entities와 같은 역할을 한다.

// interface

// export interface Movie {
//   title: string;
//   year: number;
//   genres: string[];
//   status: MovieStatus; // status의 값에는 MovieStatus에 있는 값만 사용할 수 있다.
// }

// export enum MovieStatus {
//   PUBLIC = 'public',
//   PRIVATE = 'private',
// }
